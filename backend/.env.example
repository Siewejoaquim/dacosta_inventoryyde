# PostgreSQL Database URL (Railway will set this automatically when you add PostgreSQL service)
DATABASE_URL=postgresql://postgres:password@localhost:5432/dacosta_inventory

# JWT Secret for authentication (set this to a secure random string)
JWT_SECRET=replace-with-a-long-random-string-minimum-32-chars

# Port (Railway sets this automatically)
PORT=4000
