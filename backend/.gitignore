# Dependencies
node_modules/

# Build output
dist/
build/

# ── Environment files (contain secrets — never commit these) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local

# ── Keep these (safe templates with no real secrets) ──
# .env.example  ← intentionally NOT ignored

# Seed script (contains logic that shouldn't be in production repo)
seed-admin.js

# Prisma generated (regenerated on build)
# prisma/migrations are kept — they're needed for deployment
